name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "son dep trai"
      - name: multiline scripts
        run: |
          node --version
          npm --version